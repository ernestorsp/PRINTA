
=== saveOrderClient ===
function saveOrderClient(e){e.preventDefault();const f=e.target,b=$('#saveOrderBtn'),start=Date.now(),o=Object.fromEntries(new FormData(f).entries()),isEdit=!!o.id;o._mode=isEdit?'edit':'create';o.clientKey=isEdit?'':'CLIENT-'+Date.now()+'-'+Math.random().toString(36).slice(2,10);const all=mainMaterial?[mainMaterial,...materials]:materials,costs=[...otherCosts];if(inkType!=='Sin tinta'||num(inkCost)>0)costs.push({nombre:'Tinta '+inkType,costo:num(inkCost),tipoCosto:'tinta',tintaTipo:inkType});o.materialesJson=JSON.stringify(all);o.otrosCostosJson=JSON.stringify(costs);setSpin(b,true);setTimeout(()=>closeModal('orderModal'),500);try{const old=String(o.foto||'').split('|||').filter(Boolean),fresh=[];for(const file of pendingFiles)fresh.push(await upload(file,o.id||o.clientKey,'orden'));o.foto=[...old,...fresh].join('|||');google.script.run.withSuccessHandler(async d=>{await waitMin(start,500);data=d;pendingFiles=[];renderAll()}).withFailureHandler(async x=>{await waitMin(start,500);setSpin(b,false);showModal('orderModal');showError('orderError',x)}).saveOrder(o)}catch(x){await waitMin(start,500);setSpin(b,false);showModal('orderModal');showError('orderError',x)}}

=== recalculateOrder ===
function recalculateOrder(){const map={};(data.inventory||[]).forEach(x=>map[x.id]=x);const all=mainMaterial?[mainMaterial,...materials]:materials;const cost=all.reduce((s,x)=>s+fifoItemCost(x.id,x.cantidad),0)+otherCosts.reduce((s,x)=>s+num(x.costo),0)+num(inkCost),paid=num($('#orderForm').pagado.value),g=paid-cost;$('#orderCostPreview').textContent=money(cost);$('#orderProfitPreview').textContent=(g>=0?'+':'')+money(g);$('#orderProfitPreview').className=g>=0?'gain':'loss'}

=== syncMainProduct ===
function syncMainProduct(){const f=$('#orderForm'),item=findMainInventory(f.producto.value),qty=num(f.cantidad.value)||1;mainMaterial=item?{id:item.id,cantidad:qty}:null;$('#automaticProductNote').textContent=item?`${item.nombre} × ${qty} se incluye automáticamente (${money(fifoItemCost(item.id,qty))}).`:'Este producto todavía no está vinculado al inventario.';recalculateOrder()}

=== addMaterialRow ===
function addMaterialRow(item){materials.push(item||{id:'',cantidad:1});renderMaterials()}

=== editOrder ===
function editOrder(id){const o=data.orders.find(x=>x.id===id);if(!o)return;openOrder();$('#orderTitle').textContent='Editar orden';Object.keys(o).forEach(k=>{if($('#orderForm')[k])$('#orderForm')[k].value=o[k]||''});const saved=parseJson(o.materialesJson,[]),main=findMainInventory(o.producto);mainMaterial=main?{id:main.id,cantidad:num(o.cantidad)||1}:null;materials=saved.filter(m=>!mainMaterial||m.id!==mainMaterial.id);const savedCosts=parseJson(o.otrosCostosJson,[]),ink=savedCosts.find(x=>x.tipoCosto==='tinta'||String(x.nombre||'').startsWith('Tinta '));inkType=ink?String(ink.tintaTipo||String(ink.nombre).replace('Tinta ','')).trim():'Sin tinta';inkCost=ink?num(ink.costo):0;otherCosts=savedCosts.filter(x=>x!==ink);$('#inkType').value=inkType;$('#inkCost').value=inkCost;renderMaterials();syncMainProduct()}
